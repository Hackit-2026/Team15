# プロジェクト概要

ハッカソン作品。テーマは「突破」。開発期間は3日、メンバーは学生4人（2年生3人・1年生1人）で全員が初心者。

作るもの:
講義中に「分からない」と思った瞬間に学生がスマホのボタンを押すと、その時刻が匿名で記録される。
授業後に「どこでみんなが詰まったか」がグラフで見え、自分が押した箇所はそのまま復習リストになる。

解決する問題:
大人数の講義で手を挙げるのは心理的コストが高く、学生は分からないまま流してしまう。
教員側も「質問ありますか」に反応がないため、伝わったかどうか判断できない。
この「分からないと言うコスト」をボタン1回まで下げる。

既存サービスとの差別化:
respon / iClicker / Top Hat / Understoodit など類似ツールは既に存在するが、すべて「教員が導入して教員のために使う」もの。
本作は学生が自分で使えて、学生に価値（復習リスト）が残ることを優先する。教員の導入は不要。

# 担当分け

- ヒロキ（このリポジトリでClaude Codeを使っている本人）: 学生側 student.html
- リーダー: 講義側 teacher.html
- 2年生・1年生: バックエンド Flask

# 技術構成

- バックエンド: Flask + SQLite
- フロントエンド: 素のHTML + JavaScript
  - React、npm、ビルドツールは一切使わない
  - ライブラリはCDNのみ（Chart.js、qrcode.js）
- リアルタイム更新: WebSocketは使わず、3秒ごとのポーリング（fetch）
- Flaskはテンプレートを使わず、完全にJSON APIサーバーとして動かす方針
  （現状 templates/ に教員側のHTMLがあるのは他メンバー担当分）

# ファイル構成

```
Team15/
  flask_app/
    app.py
    models.py
    controllers/
    templates/        教員側の画面（他メンバー担当）
    static/
      common.js       共通処理（ヒロキが作成）
      student.html    学生側の画面（ヒロキの担当）
      tutor/          教員側のCSS（他メンバー担当）
  requirements.txt
  README.md
```

注意: 過去にリポジトリ直下にも static/ があったが、Flaskから見えないため削除済み。
編集するのは必ず flask_app/static/ の方。

# student.html の構成

1枚のHTMLに画面3つを入れて display で切り替える。ページ遷移はしない。

- 画面1: 初回の匿名アカウント発行
  - 6桁のコードを生成して表示し、localStorage に保存する
  - 名前・メール・パスワードは一切取らない
  - 紛らわしい文字（I O 0 1）を除外した英数字を使う
- 画面2: コードで復元
  - 別端末から6桁コードを入力して記録を引き継ぐ
- 画面3: メイン
  - ボタン3つ: 分からん / もう一回 / 速い
  - 押した記録を画面下部に一覧表示（復習用）

# 主要な設計判断（勝手に覆さないこと）

匿名アカウント方式を採用した理由:
ログインを入れると「押したのが先生にバレるかも」という不安が生まれ、押されなくなる。
匿名性はこのサービスの土台なので、システム上そもそも誰か分からない状態にする。
ただし端末をまたげない問題があるため、6桁の復元コードで対応する。

押した反応はAPIの返事を待たずに先に返す:
トースト表示、バイブ、クールダウン開始を先に実行してから通信する。
返事を待つと電波の悪い会場で「押せてない」と思われて連打される。

30秒クールダウン:
完全な連打対策ではなく、全体の傾向が見えれば十分という設計。

elapsed_sec はサーバー側で計算する:
クライアントの時計を信用すると、端末ごとに時刻がずれてデータがバラバラになる。

学生側のAPIは認証なしで叩ける必要がある（学生は匿名でログインしないため）。
先生側にログインを入れる場合も、学生側APIに認証がかからないよう注意。

# API仕様（バックエンドと調整中）

以下は当初こちらから提案した形。
現在はバックエンド担当が実装したAPIに合わせる方向で調整中のため、実際の仕様は変わる可能性がある。
仕様が確定したら student.html の fetch 部分（URL・body・レスポンスの読み取り）を合わせる。

```
POST /api/sessions
  body: {"title": "情報セキュリティ"}
  → {"session_id": "..."}

GET  /api/sessions/<sid>
  → {"title": "...", "is_active": true, "elapsed_sec": 420}

POST /api/sessions/<sid>/presses
  body: {"type": "wakaran", "client_id": "ABC123"}
  → {"ok": true, "elapsed_sec": 420}

GET  /api/sessions/<sid>/summary
  → [{"minute":0,"wakaran":2,"again":0,"fast":1}, ...]

GET  /api/sessions/<sid>/presses?client_id=xxx
  → [{"type":"wakaran","elapsed_sec":420}, ...]
```

type の値は wakaran / again / fast の3種類。

# 未実装（次にやること）

1. 進捗バー
   復習画面で「8分」だけだと序盤か中盤か分からないため、100分中どの位置かをバーで表示する。
   講義は100分。maxSec は 6000 を基準にする。

2. メモ機能
   復習画面で各記録に後からメモを追加できるようにする。
   授業中はボタンを押すだけ。メモは授業後に書く（授業中にメモ欄を開くと押すコストが上がるため）。
   保存にはAPIの追加が必要（PATCH /api/presses/<press_id>）。バックエンドの余力次第。

3. API仕様への追従
   バックエンドの実装が出たら、fetch部分の URL・body・レスポンスの読み取りを合わせる。

# 意図的に作らないもの

- 学生側のログイン機能（匿名が前提）
- 授業スライドとの連携（経過時間だけで足りる。教員の導入前提に戻ってしまうため避ける）
- 過去の講義を管理する機能
- 凝った連打対策
- PC用の学生画面（スマホのみ）
- 押下の取り消し機能
- デザインの作り込み

方針: 迷ったら削る。動かないまま発表するのが最悪のケース。

# 色とラベルの定義

```
wakaran  分からん   #e05555
again    もう一回   #e0a355
fast     速い       #5580e0
```

この定義は common.js に TYPE_LABEL / TYPE_COLOR として持たせてあり、
学生側と教員側の両方が参照する。

# 発表デモの構成

1. 発表冒頭でスクリーンにQRを出し、審査員・会場に読んでもらう
2. 「発表中に分からないと思ったら押してください」と伝える
3. 通常どおり発表する
4. 最後のスライドで、会場が押したデータをグラフで表示する

保険として、会場が押してくれない場合用の仕込みデータを用意しておく。

# 開発上の注意

- 編集するファイルは必ず flask_app/static/ の方。
- Flask起動: `python flask_app/app.py` → http://127.0.0.1:5000/static/student.html?sid=test
- Live Server: http://127.0.0.1:5500/flask_app/static/student.html?sid=test
- ?sid= を付けないと「QRコードを読み直してください」で止まる。
- テスト中に初回画面へ戻すには、F12 → Application → Local Storage → client_id を削除する。
