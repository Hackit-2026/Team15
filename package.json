{
  "name": "team15-presentation-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:pdfjs": "node scripts/copy-pdfjs-assets.mjs",
    "patch:presentation-pdf": "node scripts/patch-presentation-pdf-assets.mjs",
    "build": "npm run prepare:pdfjs && vite build",
    "build:projection": "npm run prepare:pdfjs && vite build --config vite.projection.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.624",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.2",
    "vitest": "^4.0.15"
  }
}
